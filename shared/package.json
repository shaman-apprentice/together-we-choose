{
  "name": "@together-we-choose/shared",
	"private": true,
  "version": "0.0.1",
  "description": "Shared DTOs and types for backend and frontend",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rm -r dist"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "zod": "^4.1.12"
  }
}
